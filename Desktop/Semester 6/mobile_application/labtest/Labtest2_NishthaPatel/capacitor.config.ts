import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Labtest2_NishthaPatel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
